#!/bin/bash
# ============================================================================
# ONE-LINE INSTALL — copy the single command line below and paste into Terminal.
# (Everything above the line is just this note; the user only needs the command.)
#
# Default install:
T=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; curl -sL -H "Authorization: token $T" https://raw.githubusercontent.com/tyler-bam-ai/screenrecord/main/bootstrap.sh | bash
#
# Optional — 5-minute segments instead of the default: append ` -s -- -5`, e.g.
#   T=ghp_...; curl -sL -H "Authorization: token $T" https://raw.githubusercontent.com/tyler-bam-ai/screenrecord/main/bootstrap.sh | bash -s -- -5
# ============================================================================
