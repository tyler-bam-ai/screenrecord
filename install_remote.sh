#!/bin/bash
# ============================================================================
# ONE-LINE INSTALL. The repo is public, so no token is needed.
# Copy the single command line below and paste it into Terminal:
#
curl -sL https://raw.githubusercontent.com/tyler-bam-ai/screenrecord/main/bootstrap.sh | bash
#
# Optional: 5-minute segments instead of the default:
#   curl -sL https://raw.githubusercontent.com/tyler-bam-ai/screenrecord/main/bootstrap.sh | bash -s -- -5
# ============================================================================
